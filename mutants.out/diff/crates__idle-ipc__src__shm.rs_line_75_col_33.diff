--- crates/idle-ipc/src/shm.rs
+++ replace | with & in SharedMemory::create
@@ -67,17 +67,17 @@
             return Err(format!("ftruncate failed: {err}"));
         }
 
         // SAFETY: MAP_SHARED over the full sized object; fail closed on MAP_FAILED.
         let ptr = unsafe {
             libc::mmap(
                 ptr::null_mut(),
                 size,
-                libc::PROT_READ | libc::PROT_WRITE,
+                libc::PROT_READ & /* ~ changed by cargo-mutants ~ */ libc::PROT_WRITE,
                 libc::MAP_SHARED,
                 fd,
                 0,
             )
         };
         if ptr == libc::MAP_FAILED {
             let err = std::io::Error::last_os_error();
             // SAFETY: release fd + name if mapping failed.
