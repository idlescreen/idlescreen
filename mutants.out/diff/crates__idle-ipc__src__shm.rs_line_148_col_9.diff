--- crates/idle-ipc/src/shm.rs
+++ replace SharedMemory::fd -> libc::c_int with Default::default()
@@ -140,17 +140,17 @@
             fd,
             ptr,
             size,
             is_owner: false,
         })
     }
 
     pub fn fd(&self) -> libc::c_int {
-        self.fd
+        Default::default() /* ~ changed by cargo-mutants ~ */
     }
 
     pub fn ptr(&self) -> *mut libc::c_void {
         self.ptr
     }
 
     pub fn size(&self) -> usize {
         self.size
