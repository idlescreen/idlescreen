[package]
name = "idle-ipc"
version = "0.4.1"
edition = "2024"
description = "Out-of-process screensaver execution and shared memory IPC"
license = "Apache-2.0"

[dependencies]
libc = { workspace = true }
idle-api = { path = "../../idle-api" }

[dev-dependencies]
proptest = "1"
