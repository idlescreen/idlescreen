[package]
name = "trance-dbus"
version = "0.3.3"
edition = "2024"
description = "D-Bus API definitions and client for the trance screensaver daemon"
license = "MIT"

[lints]
workspace = true

[dependencies]
zbus = { workspace = true, features = ["blocking-api"] }