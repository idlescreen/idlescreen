[package]
name = "trance-gpu"
version = "0.3.3"
edition = "2024"
description = "CPU upscaling for trance screensaver frames"
license = "MIT"

[lints]
workspace = true

[dependencies]
bytemuck = { version = "1.21", features = ["derive"] }
trance-api = { path = "../../trance-api" }