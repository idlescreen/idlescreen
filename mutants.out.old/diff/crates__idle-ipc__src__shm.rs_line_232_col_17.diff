--- crates/idle-ipc/src/shm.rs
+++ replace && with || in <impl Drop for SharedMemory>::drop
@@ -224,17 +224,17 @@
                 libc::munmap(self.ptr, self.size);
                 self.ptr = ptr::null_mut();
             }
             if self.fd >= 0 {
                 libc::close(self.fd);
                 self.fd = -1;
             }
             if self.is_owner
-                && let Ok(c_name) = CString::new(self.name.as_str())
+                || /* ~ changed by cargo-mutants ~ */ let Ok(c_name) = CString::new(self.name.as_str())
             {
                 libc::shm_unlink(c_name.as_ptr());
             }
         }
     }
 }
 
 #[cfg(test)]
